name: GDPR Debug Simple

on:
  workflow_dispatch:

jobs:
  debug-gdpr:
    runs-on: ubuntu-latest
    
    steps:
      - name: Test minimal
        id: test
        run: |
          echo "🧪 Testing minimal output..."
          
          # Simulate response
          response='{"purges_executed":0}'
          echo "Response: $response"
          
          # Extract count
          count=0
          echo "Count: $count"
          
          # Write output ONCE only
          echo "count=$count" >> "$GITHUB_OUTPUT"
          echo "status=success" >> "$GITHUB_OUTPUT"
          
          echo "✅ Done"

      - name: Check outputs
        run: |
          echo "📊 Outputs received:"
          echo "  Count: ${{ steps.test.outputs.count }}"
          echo "  Status: ${{ steps.test.outputs.status }}"